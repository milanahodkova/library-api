spring:
  datasource:
    url: jdbc:postgresql://postgres-books:5431/bookdb
    username: postgres
    password: 1122
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/